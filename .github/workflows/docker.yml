# Builds and uploads docker images whenever a version tag is pushed. When a release is created,
# the associated image is then tagged with the version number and latest.

name: Docker

on:
  workflow_dispatch:
    inputs:
      pathfinder_force_version:
        description: 'Set version for pathfinder via PATHFINDER_FORCE_VERSION (defaults to `git describe --tags --dirty`)'
        type: string
        default: ''
  push:
    tags:
      - 'v*'
  release:
    types: [published]

env:
  # Workaround for https://github.com/rust-lang/cargo/issues/8719#issuecomment-1516492970
  CARGO_REGISTRIES_CRATES_IO_PROTOCOL: sparse

jobs:
  # Build a docker image unless this was triggered by a release.
  build-image:
    if: github.event_name != 'release'
    runs-on: pathfinder-large-ubuntu
    steps:
      - name: Determine Docker image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: swmansion/pathfinder
      - name: Checkout sources
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: Generate version
        id: generate_version
        env:
          PATHFINDER_FORCE_VERSION: ${{ inputs.pathfinder_force_version }}
        run: |
          if [ -n "$PATHFINDER_FORCE_VERSION" ]; then
            echo "pathfinder_version=$PATHFINDER_FORCE_VERSION" >> $GITHUB_OUTPUT
          else
            echo -n "pathfinder_version=" >> $GITHUB_OUTPUT
            git describe --tags --dirty >> $GITHUB_OUTPUT
          fi
      - name: Set up QEMU
        id: qemu
        uses: docker/setup-qemu-action@v4
        with:
          image: tonistiigi/binfmt:latest
          platforms: all
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v4
        with:
          buildkitd-config-inline: |
            [worker.oci]
              max-parallelism = 4
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      # Required for git security reasons. See https://github.com/rustyhorde/vergen/pull/126#issuecomment-1201088162
      - name: Vergen git safe directory
        run: git config --global --add safe.directory /workspace
      - name: Build
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: |
            linux/amd64
            linux/arm64
          file: ./Dockerfile
          build-args: |
            PATHFINDER_FORCE_VERSION=${{ steps.generate_version.outputs.pathfinder_version }}
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          labels: ${{ steps.meta.outputs.labels }}
          tags: swmansion/pathfinder:snapshot-${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # TODO(#3563): Remove block below and the EQLABS_* secrets once the migration window closes.
      - name: Login to Docker Hub (legacy eqlabs namespace)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.EQLABS_DOCKER_HUB_USERNAME }}
          password: ${{ secrets.EQLABS_DOCKER_HUB_ACCESS_TOKEN }}
      - name: Mirror snapshot to eqlabs/pathfinder
        run: docker buildx imagetools create -t eqlabs/pathfinder:snapshot-${{ github.sha }} swmansion/pathfinder:snapshot-${{ github.sha }}

  # Add the release labels to the associated docker image.
  tag-release:
    if: github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      - name: Tag image with release name
        run: docker buildx imagetools create -t swmansion/pathfinder:${{ github.event.release.tag_name }} swmansion/pathfinder:snapshot-${{ github.sha }}
        # Only tag the image as 'latest' if its a release i.e. not a prerelease.
      - name: Tag image with 'latest'
        if: ${{ !github.event.release.prerelease }}
        run: docker buildx imagetools create -t swmansion/pathfinder:latest swmansion/pathfinder:snapshot-${{ github.sha }}

      # TODO(#3563): Remove block below and the EQLABS_* secrets once the migration window closes.
      - name: Login to Docker Hub (legacy eqlabs namespace)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.EQLABS_DOCKER_HUB_USERNAME }}
          password: ${{ secrets.EQLABS_DOCKER_HUB_ACCESS_TOKEN }}
      - name: Mirror release tag to eqlabs/pathfinder
        run: docker buildx imagetools create -t eqlabs/pathfinder:${{ github.event.release.tag_name }} swmansion/pathfinder:${{ github.event.release.tag_name }}
      - name: Mirror 'latest' to eqlabs/pathfinder
        if: ${{ !github.event.release.prerelease }}
        run: docker buildx imagetools create -t eqlabs/pathfinder:latest swmansion/pathfinder:${{ github.event.release.tag_name }}

  # Dispatch the version bump workflow on pathfinder-infra
  update-infra:
    if: ${{ github.event_name == 'release' && !github.event.release.prerelease }}
    needs: tag-release
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch pathfinder-infra version bump
        env:
          GH_TOKEN: ${{ secrets.PATHFINDER_INFRA_DISPATCH_TOKEN }}
        run: |
          gh workflow run update-pathfinder-version.yml \
            --repo software-mansion/pathfinder-infra \
            --ref main \
            --field version=${{ github.event.release.tag_name }}
